import { HighlightTaskDirective } from './highlight-task.directive';

describe('HighlightTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTaskDirective();
    expect(directive).toBeTruthy();
  });
});
